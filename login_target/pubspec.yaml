name: login_target
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  url_launcher: ^6.2.1
  shared_preferences: ^2.2.2
  flutter_mobx: ^2.1.1
  mobx: ^2.0.1 # Adicionado mobx

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.1.2 # Adicionado build_runner
  mobx_codegen: ^2.0.1 # Adicionado mobx_codegen

flutter:
  uses-material-design: true
